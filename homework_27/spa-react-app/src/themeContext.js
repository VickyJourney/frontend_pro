import { createContext } from "react";

export const themes = {
  blue: { color: "lightblue", background: "#636373" },
  orange: { color: "orange", background: "#f3eeee" },
};

export const ThemeContext = createContext();
